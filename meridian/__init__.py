"""Onyx Meridian — the control-plane spine of the Onyx Meridian framework.

This package is the greenfield "spine" described in the framework design
(`../FRAMEWORK/`): the canonical model + AI-Employee registry + lifecycle +
governance hooks that turn the existing accelerators (Pulse, Prism, Flow,
Ensure, Vault) into a governed, cost-controlled platform.

Its job, concretely: once a department (Unit) is onboarded, **configure,
deploy, monitor, and decommission** the AI employees that run it.
"""

__version__ = "0.3.0"
