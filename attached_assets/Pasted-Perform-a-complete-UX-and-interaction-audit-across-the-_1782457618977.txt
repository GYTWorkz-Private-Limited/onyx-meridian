Perform a complete UX and interaction audit across the entire ONYX Enterprise OS.

Do NOT redesign the UI or recreate existing pages. Preserve the current Onyx design system and architecture.

Audit every page, component and route.

For every interactive element, verify that it performs a meaningful action.

This includes:
- Sidebar items
- Navigation cards
- ABU cards
- Digital Twin nodes
- Agent cards
- Mission cards
- Workflow cards
- Knowledge nodes
- KPI cards
- Charts
- Tables
- Table rows
- Status badges
- Chips
- Buttons
- Menus
- Tabs
- Breadcrumbs
- Quick Actions
- Hover actions
- Deep Dive buttons

Fix any element that:
- Does nothing
- Opens an empty page
- Shows placeholder content
- Navigates incorrectly
- Has broken routing
- Displays inconsistent data

Every click should either:
- Navigate to a relevant page
- Open a drawer
- Open a modal
- Expand details
- Open analytics
- Filter data
- Trigger a meaningful workflow

Every KPI opens detailed analytics.

Every graph opens a relevant analytics view.

Every table row opens detailed information.

Every "Deep Dive" button opens context-specific enterprise data.

Every ABU card opens its corresponding Business Unit dashboard.

Every Agent opens its Agent Overview or Agent Harness.

Every Workflow opens its Workflow/Mission configuration.

Every Recommendation opens the related business context.

Every Policy opens the appropriate Policy Studio section.

Every Log opens detailed execution information.

Every chart supports hover tooltips displaying relevant values.

Every timeline supports step selection.

Every node in Digital Twin and Knowledge Graph is clickable.

Replace all placeholder values with realistic manufacturing data that remains internally consistent throughout the application.

Perform a complete end-to-end QA pass:
- Navigate every route.
- Click every interactive element.
- Verify every modal, drawer, chart and table.
- Fix dead buttons, duplicate pages, broken navigation, empty states and placeholder actions.

Do not finish until every visible interactive component across the application behaves like a production-ready enterprise operating system.