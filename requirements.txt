# Onyx Meridian — runtime dependencies (Python 3.11+)
fastapi>=0.114,<1.0
uvicorn[standard]>=0.30
pydantic>=2.7,<3.0
pydantic-settings>=2.3
motor>=3.4           # async MongoDB driver (used only when MONGO_DB_URL is set)
httpx>=0.27          # connector HTTP transport (pull/push against platform REST APIs)
pyyaml>=6.0          # connector property files (_props.*.yaml) in the catalogue
